# GUI + packaging dependencies for the OrcaSlicer Profile Generator.
# The generator itself (generate.py / CLI) has no third-party dependencies -
# these are only needed to run gui.py or build a distributable.
PySide6>=6.7
pyinstaller>=6.6
